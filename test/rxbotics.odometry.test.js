/**
 * Tests the odometry class, reads the wheel encoders and estimates the position of robot
 */
var expect = require('chai').expect;
var RxBotics = require('rxbotics');

describe('RxBotics.Odometry', function() {
});
